<!DOCTYPE html>
<html>
<head>
  <title>Base de Datos I</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  
<nav class="navbar navbar-inverse navbar-fixed-bottom">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'Base de Datos I', root_path, class: "navbar-brand" %>
    </div>
    <ul class="nav navbar-nav">
      <% if current_user %>
        <li><%= link_to(content_tag(:span, '', class: "glyphicon glyphicon-user") + ' Mi Perfil', perfil_path) %></li>
        <li><%= link_to(content_tag(:span, '', class: "glyphicon glyphicon-cog") + ' Seguridad', '#') %></li>
      <% end %>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li><%= link_to(content_tag(:span, '', class: "glyphicon glyphicon-log-out") + ' Cerrar Sesión',
                destroy_user_session_path, method: :delete) %></li>
      <% end %>
    </ul>
  </div>
</nav>
  
<div class="container-fluid">
  
  <%= yield %>
  
</div>
</body>
</html>
