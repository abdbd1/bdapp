<% if obj.errors.any? %>
  <div class="row">
    <div class="col-xs-12">
      <h2>
        <% if obj.errors.count > 1 %>
          <%= obj.errors.count %> errores no permitieron el registro:
        <% else %>
          1 error no permitió el registro:
        <% end %>
      </h2>
      <ul>
        <% obj.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>